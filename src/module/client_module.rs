use super::base::{self, load_general_config};
use super::super::utils::{res_loader, config_loader, config_loader::Config};

pub fn client_run() {
    let config = base::load_general_config();
    println!("Config: ")
}